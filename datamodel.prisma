type Post {
  id: ID! @id
  title: String!
  published: Boolean! @default(value: false)
  authorId: String!
  prompt: Prompt @relation(link: INLINE)
}

type Prompt {
  id: ID! @id
  title: String!
  published: Boolean! @default(value: false)
  authorId: String!
  posts: [Post!]!
}
