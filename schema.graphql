type Query {
  publishedPosts: [Post!]!
  post(postId: ID!): Post
  postsByUser(userId: ID!): [Post!]!
  publishedPrompts: [Prompt!]!
  user(id: String!): User!
  login(loginId: String!, password: String!): User
  logout: String
}

type Mutation {
  createDraftPost(title: String!, userId: String!, promptId: ID!): Post
  publishPost(postId: ID!): Post
  createDraftPrompt(title: String!, userId: String!): Prompt
  publishPrompt(promptId: ID!): Prompt
  register(userName: String!, email: String, password: String!, firstName: String, lastName: String): User
}

type User {
  id: String!
  email: String
  firstName: String
  lastName: String
  posts: [Post!]!
}

type Post {
  id: ID!
  title: String!
  published: Boolean!
  authorId: String!
  author: User!
  prompt: Prompt!
}

type Prompt {
  id: ID!
  title: String!
  published: Boolean!
  author: User!
  authorId: String!
  posts: [Post!]!
}
